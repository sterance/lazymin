[package]
name = "lazymin-native"
version = "0.1.0"
edition = "2021"

[dependencies]
lazymin-core = { path = "../lazymin-core" }
ratatui = { version = "0.30", features = ["crossterm"] }
crossterm = "0.28"
